@echo off
REM setup-wizard.bat (PLANNED STUB)
REM This wizard will guide users through creating scripts\config.bat
REM and verifying AWS CLI configuration.

echo AWS EC2 QuorumStop - Setup Wizard (planned)
echo.
echo This is a placeholder. Planned features:
echo  - Detect AWS CLI and credentials
echo  - Prompt for INSTANCE_ID, AWS_REGION, KEY_FILE
echo  - Optional: discover EC2 public IP and write to config

echo For now, please configure scripts\config.bat manually.
exit /b 0
