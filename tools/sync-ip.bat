@echo off
REM sync-ip.bat (PLANNED STUB)
REM Helper to sync the current EC2 public IP into scripts\config.bat

echo AWS EC2 QuorumStop - Sync IP (planned)
echo This is a placeholder utility. Future behavior:
echo  - Query EC2 public IP via AWS CLI using INSTANCE_ID and AWS_REGION
echo  - Update SERVER_IP in scripts\config.bat safely
exit /b 0
